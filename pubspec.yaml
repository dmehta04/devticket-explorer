name: devticket_app
description: "Deutschlandticket Explorer - Interactive map for €63/month D-Ticket destinations"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_map: ^6.1.0
  latlong2: ^0.9.1
  flutter_riverpod: ^2.4.0
  dio: ^5.4.0
  google_fonts: ^6.1.0
  shimmer: ^3.0.0
  intl: ^0.19.0
  url_launcher: ^6.2.0
  cached_network_image: ^3.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
